<!DOCTYPE html>
<html lang="en">
<head>
    <title>Vigyaanrang Invitation</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/1.4.1/html2canvas.min.js"></script>
    <style>
        :root {
          --primary-color: #b78c4b;
          --accent-color: #d4b183;
          --background-color: #f9e4b7;
          --card-bg-color: #fff;
          --details-bg-color: #fff6e6;
          --text-color: #7b5e3c;
        }

        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }

        body {
          font-family: 'Playfair Display', sans-serif;
          min-height: 100vh;
          margin: 0;
          padding: 20px;
          color: var(--text-color);
          overflow-y: auto;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          background: linear-gradient(45deg, var(--background-color), #fff6e6);
        }

        .invitation-wrapper {
          position: relative;
          z-index: 1;
          display: flex;
          flex-direction: column;
          align-items: center;
          width: 100%;
          max-width: 800px;
          margin: 0 auto;
          padding: 0;
        }

        .invitation-card {
          background: var(--card-bg-color);
          border-radius: 20px;
          box-shadow: 0 10px 25px rgba(183, 140, 75, 0.15);
          padding: 30px 20px;
          text-align: center;
          width: 100%;
          max-width: 700px;
          position: relative;
          overflow: hidden;
          margin: 0 auto 20px auto;
          border: 2px solid var(--accent-color);
        }

        h2 {
          font-size: 28px;
          font-weight: 700;
          margin-bottom: 10px;
          color: var(--text-color);
          position: relative;
          z-index: 1;
        }

        .message {
          font-size: 22px;
          margin-bottom: 20px;
          color: #444;
          position: relative;
          z-index: 1;
          line-height: 1.6;
        }

        .date, .venue {
          font-size: 20px;
          margin-bottom: 10px;
          color: var(--text-color);
          position: relative;
          z-index: 1;
        }

        .closing {
          font-size: 18px;
          margin: 25px 0;
          color: #444;
          position: relative;
          z-index: 1;
        }

        .signature-label {
          font-size: 18px;
          margin-bottom: 10px;
          color: var(--text-color);
          position: relative;
          z-index: 1;
        }

        .signature {
          max-width: 180px;
          height: auto;
          display: block;
          margin: 15px auto;
          border: 1px solid var(--accent-color);
          border-radius: 8px;
          padding: 10px;
          background: #fff;
        }

        button {
          background-color: var(--primary-color);
          color: white;
          border: none;
          padding: 15px 30px;
          border-radius: 25px;
          cursor: pointer;
          font-size: 16px;
          font-weight: 600;
          transition: all 0.3s ease;
          box-shadow: 0 4px 15px rgba(183, 140, 75, 0.2);
          margin-top: 20px;
        }
        button>a{
          text-decoration: none;
          color: white;
        }

        button:hover {
          background-color: #9e7637;
          box-shadow: 0 6px 20px rgba(183, 140, 75, 0.3);
        }

        @media (max-height: 700px) {
          body {
            padding: 10px;
          }
          
          .invitation-wrapper {
            padding: 0 0 80px 0;
          }
          
          .invitation-card {
            padding: 20px 15px;
            margin: 0 auto 10px auto;
          }
          
          button {
            padding: 12px 24px;
            font-size: 14px;
          }
        }
    </style>
</head>
<body>
    <div class="invitation-wrapper">
        <div class="invitation-card">
            <h2>Dear <%= userName %>,</h2>
            <div class="message">
                With heartfelt gratitude and immense joy, we invite you to be part of the
                VIGYAANRANG FEST – a grand celebration of knowledge, creativity, and
                innovation.
            </div>
            <div class="date">April 10th, 11th, and 12th</div>
            <div class="venue">Venue: Atria</div>
            <div class="closing">Looking forward to celebrating together!</div>
            <div class="signature-label">Signed with love,</div>
            <img src="<%= userSignature %>" alt="Signature" class="signature">
            <div class="user-name" style="display: none;"><%= userName %></div>
        </div>
        <button onclick="downloadInvitation()">Download Invitation</button>
        <button><a href="record">Continue</a></button>
    </div>
    <script>window.jsPDF = window.jspdf.jsPDF;</script>
    <script src="js/app.js"></script>
</body>
</html>
